/**Array with five strings**/
const colors = ['blue','yellow','purple','brown','white'];

/**Loop with console log of each element w/index and comma**/
for (let i = 0; i < colors.length; i++) {
  console.log(i + ", " + colors[i])
}
